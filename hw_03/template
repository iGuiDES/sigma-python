Файли формується двох типів на власний вибір. Формування файлів має
здійснюватися з використанням операторів з переліку: умовних (if, elif, else),
циклічних (while і for) вибору (matching). Файл CV має бути належним чином
відформатований (рядки, пробіли, знаки пунктуації).